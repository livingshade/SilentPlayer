[package]
name = "playback_store_sqlite"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
domain = { path = "../domain" }
errors = { path = "../errors" }
rusqlite = { version = "0.40.1", features = ["bundled"] }
